# Local development. Gitignored — never commit this file.

# The booking API. Without this the site prices from bundled defaults and the
# booking flow has nowhere to send anything, so nothing reaches the admin app.
# The backend's CORS_ORIGINS must allow this site's origin (http://localhost:3000).
# VITE_API_URL=http://localhost:8080

VITE_API_URL=https://u14kij0jkg.execute-api.eu-north-1.amazonaws.com
